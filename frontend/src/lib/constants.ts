/** Margen para refrescar el access token antes de su expiración real (segundos). */
export const REFRESH_MARGIN_SECONDS = 180;

/** Duración máxima de la sesión Next-Auth (segundos). */
export const SESSION_MAX_AGE_SECONDS = 7 * 24 * 60 * 60;
